# ATDD Infrastructure Policy

Per `nw-distill` § Project Infrastructure Policy. One file per project. Apply-if-exists; write-if-absent; rewrite with `--policy=fresh`. Git history is the audit trail.

Bootstrapped: 2026-05-23 (first DISTILL — Slice 1 of `foundry-backend-mvp`).

The Architecture of Reference (port class → treatment) is unchanged here. This file specializes the concrete MECHANISM for each port in this codebase.

## Driving

| Port | Mechanism | Note |
|---|---|---|
| HTTP API (axum routes) | `reqwest::Client` against a `foundry_app::test_support::spawn_app()` helper that binds the real axum router on `127.0.0.1:0` and returns the bound `SocketAddr` | One in-process server per acceptance suite; per-scenario isolation via DB schema rotation (see Driven internal). No `WebApplicationFactory`-equivalent in Rust; bespoke `spawn_app()` is the idiom. |
| CLI (`foundry admin ...` subcommands) | `assert_cmd::Command::cargo_bin("foundry")` invoked as a subprocess from each scenario | Only used by US-06's "CLI-fallback password reset" scenario in Slice 1; expanded in later slices. |
| Docker Compose harness (US-01 only) | `std::process::Command` spawning real `docker compose -f tests/acceptance/fixtures/docker-compose.test.yml up -d` against a published-or-locally-built image | One Compose stack per `@us-01` scenario (slow: 30-60s startup). Tagged `@docker-compose` so CI can shard. See `driver.md`. |
| Multi-replica HTTP via in-process round-robin proxy (US-02 default lane, slice 3) | `reqwest::Client` against `support/round_robin_proxy.rs` (in-process axum-based proxy, ~50 LoC) fronting N `TestApp`s spawned by `support/multi_replica_harness.rs`. The proxy adds `X-Foundry-Replica` to every response so tests can introspect routing. | Per-scenario N replicas + one proxy; all share ONE per-scenario Postgres schema (matches production). Cheap (~250ms per scenario). |
| Multi-replica HTTP via real Caddy + docker-compose (US-02 production-shape lane, slice 3) | `std::process::Command` spawning `docker compose -f tests/acceptance/fixtures/slice3/docker-compose.multi-replica.yml up --scale foundry=3 -d` against a real `caddy` container + 3 published-or-locally-built foundry images. | One scenario only, tagged `@docker-compose @us-02 @manual-trigger`. ~35s per scenario; CI matrix opt-in. |
| CLI subcommand `foundry doctor backup-verify` (US-03, slice 3) | `assert_cmd::Command::cargo_bin("foundry")` subprocess invocation against a dump file produced by the per-scenario harness. | Driving adapter coverage for the doctor subcommand per RCA-fix P1 (subprocess invocation of the user-facing entry, not direct service call). |
| CLI subcommand `foundry doctor provision-workspace` / `grant-super-admin` / `revoke-super-admin` (multi-workspace-provisioning, ADR-002) | `assert_cmd::Command::cargo_bin("foundry")` subprocess invocation with `DATABASE_URL` pointing at the per-scenario testcontainers schema; asserts structured exit code + stdout (new workspace id, first-admin invite link). | Reuses the allow-listed `run_restore_comment` scaffold (thread-isolated tokio runtime, live-DB sqlx, structured exit codes). `admin_cli` stem ALREADY LAYER-1e allow-listed (`check_arch.rs:394`) — no new guard entry for v1. Driving adapter coverage per RCA-fix P1: subprocess invocation of the operator entry, not a direct `create_workspace` use-case call. |
| JSON API (`/api/v1/...`) — Feature A "Programmatic Foundry" (web-tier-extraction, US-W05a/b/c) | `reqwest::Client` against the SAME `foundry_app::test_support::spawn_app()` in-process router used for the HTML API, sending `Authorization: Bearer <jwt>` (+ JSON bodies). The `/api/v1` sub-router is `.merge()`-d into `build_router` by DELIVER. | Same in-process server + per-scenario schema as the HTML driving port; no new harness. The bearer credential is a real EdDSA JWT minted with the fixed test signing key (see Driven external). |
| Boundary guard (`cargo xtask check-arch` + `cargo-deny`) — Feature A (US-W06) | `std::process::Command` running `cargo run -p xtask -- check-arch` against the real workspace root (and, for violation scenarios, a throwaway tree copy with a planted violation, staged like `support/test_migration.rs`). | Subprocess against the source tree, NOT the foundry binary. Tagged `@boundary-guard` so CI shards it onto the lint lane (no Postgres). |
| Static-asset route (`/static` via `tower_http::ServeDir`) — Feature B "htmx Web Tier" (US-B02/B05) | `reqwest::Client` GET against the SAME in-process `spawn_app` router used for the HTML/JSON surfaces; the route `.nest_service("/static", ServeDir::new("static"))` reads `crates/foundry-app/static/` from the real filesystem. | Same in-process server + per-scenario schema as the HTML driving port; no new harness (DESIGN: build_router/spawn_app unchanged). GET-only, CSRF-exempt (safe method), path-traversal-safe by construction. Asserted via `b_asset_*` world fields. |
| Instance-admin web surface (`GET /admin/instance/workspaces`, `POST /admin/instance/workspaces`, `POST /admin/instance/super-admins`) — web-provisioning-flow (ADR-001/002) | `reqwest::Client` against the SAME `foundry_app::test_support::spawn_app()` in-process router, with a REAL signed-in `foundry_session` cookie (the shipped session layer) and a REAL double-submit `_csrf` token on the POSTs; asserts rendered HTML page/fragment substrings, HTTP refusal status + BYTE-IDENTICAL refusal body, and post-provision DB row presence. | Same in-process server + per-scenario schema + real session/CSRF as the shipped HTML driving port; NO new harness. The new routes mount UNDER the shipped `csrf_middleware` + `session_layer`. New driven-internal read `list_workspaces()` (D4) hits the same per-scenario Postgres. `instance_admin` stem added to LAYER-1e allow-list (D6). Provisioning use-case + grant + `is_instance_admin` are SHIPPED driven-internal (already in this policy under multi-workspace-provisioning) — REUSED verbatim, never faked. |
| Member-invite issuance web surface (`GET /workspace/invites`, `POST /workspace/invites`) — workspace-member-invites (ADR-001, D1/D2) | `reqwest::Client` against the SAME `foundry_app::test_support::spawn_app()` in-process router, with a REAL signed-in `foundry_session` cookie and a REAL double-submit `_csrf` on the POST; asserts the rendered admin form / "invite sent" fragment substrings (link + 7-day validity), HTTP refusal status + BYTE-IDENTICAL non-enumerable-404 body for non-admins/signed-out, and the post-issue `invites` row. | Same in-process server + per-scenario schema + real session/CSRF as the shipped HTML driving port; NO new harness. Routes mount UNDER `csrf_middleware` + `session_layer`, admin-gated INSIDE the handler by the SHIPPED `is_workspace_admin` (NFR-1). NO new LAYER-1e allow-list line (D7 — the handler scopes the workspace from `SessionUser`, not request input). Reuses the SHIPPED `insert_invite` (created_by = inviter), `InviteToken::new`, `resource_not_found_page()`. |
| Instance-admin project rename web surface (`GET /admin/instance/workspaces` project listing, `POST /admin/instance/projects/{project_id}/rename`) — instance-admin-project-rename (D1-D7, ADR-PROJECT-RENAME-001/002) | `reqwest::Client` against the SAME `spawn_app()` in-process router, with a REAL signed-in `foundry_session` cookie and a REAL double-submit `_csrf` on the POST; asserts the rendered `data-project-row`/`data-project-empty` page markers, the 200 bare row fragment, the 422 bare error fragment (marker `project-rename-error`, three D4 messages verbatim), and the BYTE-IDENTICAL uniform-404 refusal for non-admin / signed-out / garbled-id / unknown-id. Board-survival oracle reads the STORED slugs from the per-scenario Postgres BEFORE the rename (never re-derived from a name — the D2 trap). The htmx row swap + `form-errors.js` error-slot routing are asserted in the `@needs-browser` fantoccini lane (real headless Chrome against the same origin — the HTTP lane is byte-blind to the swap). | Same in-process server + per-scenario schema + real session/CSRF as the shipped instance-admin surface; NO new harness. Route mounts UNDER `csrf_middleware` + `session_layer` (never `/api/v1`). `instance_admin` stem stays LAYER-1e allow-listed; the new instance-wide read is named `list_projects_for_instance` so its cross-tenant scope is explicit. |
| Lane-delete web surface (`GET`+`POST /team/{team_slug}/project/{project_slug}/lanes/{lane_slug}/delete`) + lane-consuming board/dialog/report reads — board-lane-management (D7-D10, ADR-BOARD-LANE-001/002) | `reqwest::Client` against the SAME `spawn_app()` in-process router, with a REAL signed-in `foundry_session` cookie and a REAL double-submit `_csrf` on the confirm POST (the dialog GET is a SAFE read and carries none — DESIGN refinement 3); asserts the `[data-modal="delete-lane"]`/`[data-lane-count]`/`select[name=destination]`/`button[name=fate]` dialog markers, the OOB `id="board-columns"` refresh fragment, the 422 bare `delete-lane-error` fragment ("A board needs at least one lane"), and the BYTE-IDENTICAL uniform-404 refusal for non-members/signed-out on BOTH verbs. Board-render oracles read the expected lane list BACK FROM the per-scenario Postgres `lanes` rows — never a test-local static column list (the D8 trap). Machine-client legs (per-project 422 through the shared `change_issue_state` seam; leftmost-landing echo) reuse the Feature-A fixed-EdDSA-bearer idiom against `/api/v1`. The htmx dialog swap, fate-button submitter inclusion, OOB column refresh, and the declarative `data-action="close-modal"` close are asserted in the `@needs-browser` fantoccini lane. | Same in-process server + per-scenario schema + real session/CSRF as every board surface; NO new harness. Routes mount UNDER `csrf_middleware` + `session_layer`, non-member -> uniform 404 on both verbs (deliberate asymmetry vs the board page's 403 — DESIGN refinement 4). No new LAYER-1e line (slug-resolved paths, not workspace ids). |
| Member-invite accept web surface (`GET /invites/accept`, `POST /invites/accept`) — workspace-member-invites (ADR-002/003, D3/D4/D5) | `reqwest::Client` against the SAME PUBLIC (signed-out-accessible) `/invites/accept` route pair the shipped invite-accept-flow drives, with a REAL double-submit `_csrf` minted on the GET; asserts the set-password form substrings, the 303 redirect + landed tenant, the BYTE-IDENTICAL uniform `invite_refusal_page()` across {expired, used, tampered, unknown, email-collision}, and post-consume `invites.used_at` set exactly once. | Same in-process server + per-scenario schema + real session/CSRF as the shipped invite-accept driving port; NO new harness. The POST adds an internal data-derived kind DISPATCH (D3) — first-admin (SHIPPED `set_first_admin_password_and_consume`) vs member (NEW `create_member_and_consume`). NO new LAYER-1e allow-list line (D7). |
| Theme surface (rendered `<head>` + `/static/css/foundry.<hash>.css` + `/static/js/theme.js`) and the five `check-arch` seam/asset rules — canzan-theme-system (D-03/D-06/D-09, ADR-CANZAN-THEME-003/004) | Two lanes. **HTTP lane**: `reqwest::Client` against the SAME `spawn_app()` in-process router — asserts served `<head>` markup facts (the theme `<script>` present in `<head>` carrying NO `defer`/`async`/`type=module`; the `theme-color` media pair; both manifest keys still declared) and the served stylesheet bytes. **`@needs-browser` fantoccini lane**: real headless Chrome with THREE session capabilities composed on the shipped `open_session` — `ColorScheme::Dark` (`--force-dark-mode` injected into `goog:chromeOptions.args`), `SiteStorage::Refused` (`profile.default_content_setting_values.cookies = 2`) and the shipped `Scripting::Disabled` — yielding `new_dark_session`, `new_dark_session_without_scripting`, `new_dark_session_refusing_site_storage`, plus the `device_prefers_dark()` guard and the `unhandled_script_errors()` reader. Contrast is COMPUTED in the live browser (`getComputedStyle` + an ancestor walk for the first non-transparent background), never restated from the ratios recorded in the token comments. The five guard rules' injected-violation gold tests are `xtask` unit tests calling the rule functions against staged tree copies. | NO new harness crate and NO new dependency. **Both capabilities were MEASURED, not assumed** (chromedriver 151.0.7922.138, real `http://` origin): `--force-dark-mode` flips BOTH `matchMedia` and the computed custom property, so the cascade genuinely applies, while `--enable-features=WebContentsForceDark` measurably does NOT work (recorded so nobody later "fixes" the flag to it); `cookies = 2` makes both READ and WRITE of stored state throw `SecurityError`, baseline being that both succeed. The colour preference is set at SESSION CREATION rather than via CDP `Emulation.setEmulatedMedia` — fantoccini 0.21.5 DOES expose `issue_cmd`/`session_id`, so CDP was reachable and was rejected on DETERMINISM (a runtime call can race page load; a capability cannot) — matching the `open_mobile_session` idiom. **Anti-vacuity is mandatory in this lane**: the baseline is `false`/LIGHT, so a dark scenario that stamps an explicit choice is green whether or not the `@media (prefers-color-scheme: dark)` block exists; every dark-by-device `Given` calls `device_prefers_dark()` before asserting anything, and the `@oracle-probe` scenario asserts BOTH arms and is un-pended first. The unhandled-error recorder is a `goog:loggingPrefs` SESSION capability, not an in-page `window.onerror`, so it is armed before the first navigation and catches an error thrown while a `<head>` script is still being parsed. Divergence from the boundary-guard row above: this feature's gold tests call the rule functions DIRECTLY rather than driving `cargo run -p xtask -- check-arch` as a subprocess (Decision 4 placed infrastructure guards outside acceptance scope), and the recorded consequence is a surviving `run -> ExitCode` mutant whose only guard is `cargo xtask ci` itself. |

| Lane-move web surface (`POST /team/{team_slug}/project/{project_slug}/lanes/{lane_slug}/move`) + the column-header pointer drag — board-lane-reorder (D2/D3/D7, ADR-BOARD-LANE-006/007) | Two lanes. **HTTP lane**: `reqwest::Client` against the SAME `spawn_app()` in-process router with a REAL signed-in `foundry_session` cookie and a REAL double-submit `_csrf`; asserts the OOB `id="board-columns"` refresh, the BYTE-IDENTICAL uniform-404 across {vanished mover, vanished neighbour, non-member, signed-out}, and lane rows read BACK from the per-scenario Postgres. The signed-out arm posts a CSRF pair **without** a session, so it is judged by the handler's authz arm rather than stopped at 403 by the middleware — posting an empty cookie header instead would assert nothing about authorization. **`@needs-browser` fantoccini lane**: real headless Chrome for the drag itself, dispatching genuine `PointerEvent`s (`pointerdown` → 8 × `pointermove` → `pointerup`) because a threshold-gated implementation must SEE movement; `open_mobile_session` at 390px for the `@mobile` touch-drag and edge-auto-scroll scenarios. | Same in-process server + per-scenario schema + real session/CSRF as every board surface; NO new harness. POST-only route (a move needs no dialog, D12) mounted UNDER `csrf_middleware` + `session_layer`, alongside the shipped edit/insert/delete lane routes, and mounted AT DISTILL as a clean 501 for the same honesty reason those were: an unrouted path answers the exact uniform 404 the authz scenarios assert and would pass for the wrong reason. **Pointer Events, not HTML5 DnD** (ADR-BOARD-LANE-007): `dragstart`/`drop` emit NOTHING on touch, so the shipped `board-dnd.js` mechanism cannot be reused for lanes and cannot satisfy the touch KPI. The two drag systems share a DOM region and no code — the boundary is which element the gesture starts on — so the shipped card-drag scenarios must pass UNMODIFIED as its standing proof; one scenario here asserts that boundary from the lane side, and it is deliberately GREEN at DISTILL. The card-drag oracle POLLS the store rather than reading it straight after the drop, because `board-dnd.js` commits through an async `fetch` and a direct read races it (observed). |

## Driven internal (real)

| Port | Mechanism | Note |
|---|---|---|
| `PgPool` (Postgres) — Slice 1 schema (workspaces, users, teams, projects, issues, bootstrap_tokens, invites, outbox, signin_attempts, tower_sessions) | **One shared `testcontainers-rs` Postgres 16 container per `cargo test` invocation**, plus **per-scenario schema** — each scenario gets `CREATE SCHEMA test_<uuid>` + `SET search_path` + run migrations. Schema dropped at scenario teardown. | Decision: shared container + per-scenario schema is the **speed-vs-isolation pivot** for Slice 1. Truncating tables is too entangled with FK cascades + outbox; transactional rollback fights tower-sessions's own transaction; fresh-DB-per-scenario costs 200ms/scenario × 25 scenarios = 5s overhead → ruled out. Per-schema rotation costs ~30ms/scenario. |
| `tower_sessions_sqlx_store::PostgresStore` | Same Postgres as above; tower-sessions's own migrator runs once per test schema | Sessions are first-class observable state in US-06; the store is real, never mocked. |
| `bootstrap_tokens`, `invites`, `signin_attempts` tables | Same Postgres | These are state surfaces the assertions read directly via a thin `tests/acceptance/support/db_introspect.rs` helper (read-only SELECTs); never used to bypass the driving port for writes. |
| Postgres advisory lock (`pg_advisory_lock(MIGRATION_LOCK_ID)`) — US-04 slice 3 | Real Postgres advisory lock observed via `foundry_store::run_migrations` / `run_migrations_from_dir`; `MultiReplicaHarness::spawn_concurrent` boots N replicas with `tokio::join_all` so the lock race is the actual production race. | Two replicas per scenario; per-scenario test migrations staged via `support/test_migration.rs` in a `tempfile::TempDir`. |
| `issue_attachments` bytea (US-11 slice 3) | Real `sqlx::PgPool` against the same per-scenario schema; uploads via real `axum::extract::Multipart`, downloads via real bytea SELECT. Round-trip integrity verified via in-memory sha256 against the uploaded bytes. | Slice-3 adds migration `0004_issue_attachments.sql`; bytea content lives in the same per-scenario schema as the rest of slice-1+2 state, so US-03's pg_dump scenario inherits attachment coverage without special wiring. |
| System `pg_dump` / `pg_restore` (US-03 slice 3) — **CONTAINERISED 2026-09-04** | `std::process::Command` running `docker run --rm postgres:16-alpine pg_dump/pg_restore` — the SAME image tag the server containers use, so the client can never be older than the server. Reaches the testcontainers ports via `--add-host=host.docker.internal:host-gateway`; the dump dir is bind-mounted at `/backup` and `--user $(id -u):$(id -g)` keeps dump files host-owned (without it the container writes root-owned files that the host-side `truncate_dump` cannot open, and the corrupt-archive scenario fails for a permissions reason that looks nothing like its subject). Per-scenario second testcontainers Postgres is still the restore target. | **No host Postgres client is required** — Docker already is. This replaces the previous host-PATH dependency and its whole class of failure: a Homebrew `pg_dump 14.24` refuses a 16.14 server outright, which took all six US-03 scenarios down and could not be fixed by a version check, only by removing the host's freedom to differ. The `cargo xtask ci` preflight that demanded `pg_dump >= 16` is retired with it. The SHIPPED `foundry doctor backup-verify` legitimately shells out to `pg_restore` twice and is NOT stubbed — those calls are what two scenarios exist to prove. It resolves the program through `FOUNDRY_PG_RESTORE` (default `pg_restore`), and the lane points that at a generated shim which translates host dump paths to `/backup/...` and loopback URLs to `host.docker.internal`, forwarding every other argument untouched. Docker is deliberately NOT put in the server: the runtime image is distroless with no docker client, and a server shelling out to a container daemon to read a file is a larger dependency than the one it removes. |
| `machine_tokens` registry / `jti` denylist (Feature A, US-W05b, migration `0007`) | Same shared `testcontainers-rs` Postgres + per-scenario schema; the `jti`-keyed registry is a REAL table (stores no secret — only issuance/lifecycle metadata). Revoke/expire/forged paths exercise real rows. | Driven-INTERNAL (shared state), so REAL per the Architecture of Reference — never faked. DELIVER adds the migration + `Store` repo methods (`insert/find_by_jti/revoke/list/touch`). |
| `instance_admins` role table + `is_instance_admin(user_id)` authz (multi-workspace-provisioning, migration `0011`) | Same shared `testcontainers-rs` Postgres + per-scenario schema; `instance_admins(user_id PK)` is a REAL table seeded by the bootstrap claim / `grant-super-admin` and read by the real `EXISTS`-shaped `is_instance_admin`. Provisioning refusal paths exercise real (absent) rows. | Driven-INTERNAL (shared state), so REAL per the Architecture of Reference — never faked. Mirrors `is_workspace_admin`'s shape but instance-scoped. DELIVER adds the migration + `Store::is_instance_admin` + the `provision_workspace` tx. |
| `Store::create_member_and_consume` one-TX (workspace-member-invites, ADR-002/004, NO migration) | Same shared `testcontainers-rs` Postgres + per-scenario schema. The NEW one-TX seam (guarded-UPDATE consume → INSERT user → INSERT `member` membership → set `used_by` → COMMIT; on `users.email_lower` UNIQUE violation → ROLLBACK → `EmailCollision`) runs against the REAL per-scenario schema. The `invites.used_at`/`used_by`, `users.email_lower UNIQUE`, `workspace_memberships.role CHECK` columns are SHIPPED in `0001_init.sql` — ZERO migration. Concurrency scenario boots two concurrent accept POSTs against one live invite to exercise the real guarded-UPDATE race. | Driven-INTERNAL (shared state, atomic), so REAL per the Architecture of Reference — never faked. The collision arm is asserted by READING the response (uniform refusal, NOT a 500) + the DB (no second user, invite unconsumed) via the read-only `db_introspect.rs` helper. `is_workspace_admin`, `insert_invite`, `set_first_admin_password_and_consume`, `resolve_active_workspace`, `InviteToken`, `hash_password`, `check_password_policy` are SHIPPED driven-internal — REUSED verbatim. |
| Pre-feature migration snapshot (multi-workspace-provisioning slice 5, ADR-004 real-snapshot proof) | `support/test_migration.rs` `TestMigrationsDir` precedent: stage the pre-0009 migration set (`0001`..`0008`) into a `tempfile::TempDir`, seed representative tenant data via the real `Store`, snapshot row counts + per-row content for the tenant tables, then apply `0009`/`0010`/`0011` from the canonical dir and re-snapshot. | Driven-INTERNAL — the migration runs against a REAL per-scenario Postgres schema through the SAME `run_migrations_from_dir` the production boot path uses. The "pre-feature snapshot" is a staged migration history, not a fake. No backfill migration exists (D4); the proof asserts before/after equality + workspace id unchanged + carried session/token still resolves. |
| `lanes` table + `Store::{list_project_lanes, delete_lane_with_fate}` + composite FK `(project_id, state) → lanes (project_id, slug)` (board-lane-management, migration `0015` — DELIVER-owned) | Same shared `testcontainers-rs` Postgres + per-scenario schema. Lane rows are REAL rows: scenario Givens seed the post-0015 grandfather/creation shapes directly; the two-fate delete runs the REAL single transaction (FOR UPDATE + FK strand-guard + bounded retry) against the per-scenario schema. The zero-laneless guard query (`architecture-design.md` §4) runs after every mutating scenario. The migration backfill/idempotency oracle uses the `TestMigrationsDir` precedent: `stage_subset(14)` into a `tempfile::TempDir`, seed pre-0015-shaped data (incl. one `cancelled` issue), then run the CANONICAL migrations dir TWICE through the same `run_migrations_from_dir` the production boot path uses, asserting grandfathered lanes, zero issue-row rewrites, and `fk_issues_lane` in `pg_constraint`. | Driven-INTERNAL (shared state, atomic), so REAL per the Architecture of Reference — never faked. `insert_project` lane seeding + `insert_issue_with_outbox` leftmost-landing are DELIVER-owned behaviour changes to SHIPPED driven-internal ports (observed through the driving ports, not faked). `delete_issue_cascade` shape REUSED for the delete fate (comments/attachments/events cascade — asserted by zero-count in the per-scenario schema). |
| `Store::{rename_lane, insert_lane_at}` + `foundry_core::lane_slug` (board-lane-overflow-menu, **NO migration** — the schema counter stays at 0015) | Same shared `testcontainers-rs` Postgres + per-scenario schema. Lane rows are REAL rows; the insert runs the REAL single transaction (`FOR UPDATE` on the project's lanes → anchor resolved by identity inside the lock → position captured before the shift → slug pre-check → plain `UPDATE` shift → insert) against the per-scenario schema. A dedicated `@concurrency` scenario drives TWO REAL concurrent confirms at one anchor through the real adapter, because the unguarded path hands the loser a raw `duplicate key` error — measured during the DESIGN spike, not hypothesised. Contiguity (`0..n-1`) is asserted by the suite, since Postgres enforces only uniqueness and a gap would be invisible to the schema. The zero-laneless guard query runs after every mutating scenario. | Driven-INTERNAL (shared state, atomic), so REAL per the Architecture of Reference — never faked. **No new migration**: `UNIQUE (project_id, position) DEFERRABLE INITIALLY IMMEDIATE` (shipped in `0015`) checks at end-of-**statement**, so a plain bulk shift commits; the identical statement against a non-deferrable constraint fails. That keyword is therefore load-bearing for this feature (ADR-BOARD-LANE-003) and a later migration dropping it would break insert while every existing test stays green. |
| Vendored static blobs (htmx/Alpine/CSS under `static/vendor` + `static/css`) — Feature B (US-B02/B05) | Real filesystem read by `ServeDir` from the committed `crates/foundry-app/static/` dir (the blobs ship in the repo + the image via Dockerfile `COPY`). | Driven-INTERNAL (shared, deterministic, local resource), so REAL per the Architecture of Reference — the `@real-io @adapter-integration` US-B02 scenario fails if the blob is absent (proving the served bytes are the real vendored artifact, not a stub). |
| Derived font blobs (`static/fonts/*.woff2`) + the `VENDOR.md` provenance rows — canzan-theme-system (ADR-CANZAN-THEME-001/002/003) | Real filesystem read by the SAME `ServeDir` route as the row above; the three axis-instanced, latin-subset OFL faces (76,416 B total) ship in the repo and in the image. Integrity is machine-checked by `check-arch` **R2** (every `<stem>.<8hex>.<ext>` filename equals the file's own sha256 prefix) and **R3** (every `VENDOR.md` row's recorded sha256 recomputes), both deriving their input set by SCANNING rather than from a maintained list, so the guard cannot itself go stale. | Driven-INTERNAL (shared, deterministic, local resource), so REAL per the Architecture of Reference — never faked, and a CDN-hosted face would be a driven-EXTERNAL port `VENDOR.md:4` prohibits outright. **Provenance is a separate, WEAKER claim than integrity** (ADR-002): re-derivation from the pinned upstream with the pinned toolchain is EXPECTED and explicitly NOT guaranteed byte-for-byte, because woff2 output is compressor-dependent — so the Tier-2 audit anchors on the compressor-independent intermediate TTF sha256 instead. Two flags make that anchor stable, and removing either silently breaks the audit while every test stays green: `SOURCE_DATE_EPOCH` (`varLib.instancer` stamps `head.modified` from the wall clock — 5 bytes differed between two runs seconds apart on one machine) and `--no-optimize` (the IUP optimiser's float-tolerance choice differed across platforms on 4 of 414 JetBrains Mono glyphs while the font was provably identical). Recipe: `tools/fonts/derive-fonts.sh`, run offline, NEVER a build step. |

| `Store::move_lane_before` + `LaneMoveOutcome` (board-lane-reorder, **NO migration** — the counter stays at 0015) | Same shared `testcontainers-rs` Postgres + per-scenario schema. The move runs the REAL single transaction (`FOR UPDATE` on the project's lanes → BOTH ends resolved by identity inside the lock → ONE `UPDATE … SET position = CASE …` statement) against the per-scenario schema. A dedicated `@concurrency` scenario drives TWO REAL concurrent moves by TWO different members (`tokio::join!`) through the real adapter. Contiguity (`0..n-1`) is asserted by the suite; identity (`slug`, `label`) is compared before/after from the STORE; the zero-laneless guard runs after every mutating scenario. | Driven-INTERNAL (shared state, atomic), so REAL per the Architecture of Reference — never faked. **The concurrency oracle asserts the resulting ORDER, never "no error was raised"** — this INVERTS the predecessor's insert oracle and is the single most important line in this row. ADR-BOARD-LANE-006 Finding 4 MEASURED that two unlocked concurrent moves raise NO error, keep contiguity, keep uniqueness, keep zero laneless issues, and STILL leave a lane neither operator mentioned shoved past another (5/5). A status-code assertion is green on that case; so is a contiguity assertion. The scenario's expected order is chosen to be SERIALISATION-INDEPENDENT (the two moves commute, so A-then-B and B-then-A agree) while still differing from the measured stale-read result — deterministic AND discriminating. **No new migration**: `UNIQUE (project_id, position) DEFERRABLE INITIALLY IMMEDIATE` (0015) is a PRECONDITION for every candidate shape — the single-statement permutation, the sentinel park, and `SET CONSTRAINTS … DEFERRED` all fail against a non-deferrable twin (measured, the last with `constraint "…" is not deferrable`). |

## Driven external / non-deterministic (fake)

| Port | Fake | Note |
|---|---|---|
| `lettre::SmtpTransport` (SMTP for email invites in US-05, password reset in US-06) | `FakeEmailSender` — in-memory `Vec<SentEmail>` exposed via `AppState::test_outbox()`; tests assert message count + headers + body fragments | Real SMTP (GreenMail/MailHog container) is on the slice-3 roadmap per `auth.md`'s contract-test note. Slice 1 uses the in-memory fake; the `FakeEmailSender` enforces the same input validation as `lettre::SmtpTransport` (assert `to`, `from`, `subject` non-empty) per the "test doubles must validate inputs" rule. |
| `tokio::time::sleep` (used by NFR-SEC-02 brute-force delay) | `FakeClock` exposed via `AppState::time` injection — production wires real `tokio::time::sleep`; tests wire a `MockClock` that records sleep durations and returns immediately | Decision: assert that the 6th attempt's recorded delay is ≥4500ms WITHOUT actually waiting 5s in CI. The latency assertion is on the recorded duration, not wall-clock. Justification: NFR-SEC-02 is a CORRECTNESS contract ("delay was scheduled"), not a wall-clock contract; wall-clock test would add 5s × N scenarios to the suite. |
| Bootstrap token generator (`rand::random` over 32 bytes) | Production randomness in tests; the assertion is on token *shape* (length, URL-safety, single-use), not on a specific value | The HMAC and SHA-256 paths are deterministic given the input; no fake needed. |
| Clock for `expires_at` (HMAC tokens, sessions, signin_attempts windowing) | Same `FakeClock` as above; production uses `std::time::SystemTime`; tests override via injection | Required so US-05's "expired token rejected" scenario can advance time without sleeping. |
| Per-replica health-state injection for US-02 "DB unreachable" scenario (slice 3) | Test-only `AppState::mark_db_unreachable()` flag (gated behind `cfg(any(test, feature = "test-hooks"))`) that causes `/readyz` to return 503 even when the real PgPool is healthy. | Strictly more honest than "drop the PgPool" since the real production replica's behaviour on DB outage is "Postgres unreachable -> /readyz 503", and we cannot reliably kill the testcontainers Postgres mid-scenario without affecting sibling scenarios. The flag is the cleanest seam. |
| Per-replica `X-Foundry-Replica` header (slice 3, US-02 introspection) | Production replicas do NOT add this header. Test-only middleware injected by `MultiReplicaHarness::spawn` adds it from `AppState::test_replica_addr` (`Option<SocketAddr>`, `None` in production binaries). | The header is a test affordance for round-robin-distribution + SSE-landing-replica assertions; production responses are unchanged. |
| Ed25519 machine-token signing keypair (Feature A, US-W05b) | A FIXED test keypair set on `AppState.machine_token_verifier` (and a `MachineTokenSigner` in the harness) at build, mirroring the fixed `session_secret` test value. The EdDSA sign/verify is REAL crypto — only the key MATERIAL is a known test fixture so scenarios can mint valid/expired/wrong-alg tokens deterministically. | Driven-external/non-deterministic CLASS (a credential secret), so the key material is a fixture — but the crypto path itself is exercised for real (alg-pin + signature verify are the behaviour under test). DELIVER adds `MACHINE_TOKEN_PUBLIC_KEYS`/`MACHINE_TOKEN_SIGNING_KEY` wiring (parallels `SESSION_SECRET`). |
| Token `exp` clock (Feature A, US-W05b expired-credential scenario) | Reuse the existing `FakeClock` (`MockClock`) to mint a token whose `exp` is already in the past, or to advance time past a live token's `exp`. | Same FakeClock seam used for session/signin-attempt windowing; no new fake. |
| Rate-bucket eviction clock (multi-workspace-provisioning slice 6b, ADR-005) | Reuse the SHIPPED clock seam (`ClockedRevokeGuard` / `MockClock`) that `RevokeRateLimiter::consume(now)` already takes. Eviction reads the SAME `now`; tests advance the `MockClock` past the idle window `W = ceil(C/R)s` to drive idle eviction and assert the map stays bounded while an ACTIVE principal's throttle is byte-identical with/without eviction. | Driven-external/non-deterministic CLASS (a clock), so the time source is a fixture — but the eviction arithmetic is exercised for real. ZERO new crate (`std::collections::HashMap::retain` over the existing `Mutex`). The bounded-map + behaviour-preserving claims are unit/property tests at the `rate_limit` module (layer 1-2), NOT acceptance scenarios — noted here only because the clock seam is the shared fake. |
