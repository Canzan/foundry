# SUPERSEDED — renumbered by the 2026-05-30 driver correction

This brief was "Slice 1 — board extraction (walking skeleton)" under the OLD web-first slice
order. The driver correction (see `wave-decisions.md` D7/D8) made the JSON API the headline
outcome and flipped the slice order.

- The **new Slice 1** is the JSON read walking skeleton → `slice-01-json-read.md`.
- The board-extraction work is now **Slice 3** → `slice-03-board-extraction.md`.

This file is retained only as a redirect; do not use it for planning.
