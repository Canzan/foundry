{
  "roadmap": {
    "project_id": "multi-workspace-tenancy",
    "feature_id": "multi-workspace-tenancy",
    "created_at": "2026-06-09T00:00:00Z",
    "total_steps": 13
  },
  "phases": [
    {
      "id": "01",
      "name": "Slice 1 — walking-skeleton coexistence + fail-closed resolution",
      "steps": [
        {
          "id": "01-01",
          "name": "Forward-only 0009 migration drops uniq_one_workspace + the walking-skeleton coexistence proof (Acme sees only Acme)",
          "criteria": [
            "a NEW forward-only migration crates/foundry-store/migrations/0009_multi_workspace.sql DROPs the shipped uniq_one_workspace unique index (0001_init.sql:15) so a second `workspaces` row can be INSERTed; the migration edits no prior migration file and rewrites/moves/deletes NO existing row (dropping an index touches no row data, ADR-006)",
            "the existing workspace keeps its id and all `workspace_id` FKs unchanged — the migration is purely additive/subtractive at the index level (CREATE TABLE instance_admins from ADR-006 §2 MAY ride along in 0009, additive + empty, but is Slice-6-consumed and harmless here)",
            "with 0009 applied via the per-scenario schema set, the Background's SECOND `INSERT INTO workspaces` (\"Globex\") succeeds where it RED-failed before, and a member of Acme listing the \"Auth\" project's issues over GET /api/v1/teams/{team}/projects/{project}/issues receives 200 with ONLY the Acme issues (ACME-1, ACME-2)",
            "no Globex issue (no GLOBEX- key) appears in the Acme answer — proving the shipped per-table `workspace_id` scoping is genuinely load-bearing once a second tenant exists (the slice's riskiest assumption)",
            "the @walking_skeleton tag's scenario authenticates end-to-end against the real /api/v1 router via the InProcHarness (real HTTP, real EdDSA bearer bound to (marco, Acme), real testcontainers Postgres) — answering 'two real workspaces coexist and a request resolves to exactly its own workspace on one read path'"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-01-coexist.feature",
          "scenario_name": "A member of one workspace lists only their own workspace's issues",
          "estimate_hours": 4,
          "deps": [],
          "implementation_scope": {
            "files": [
              "crates/foundry-store/migrations/0009_multi_workspace.sql",
              "crates/foundry-acceptance/tests/features/us-mwt-slice-01-coexist.feature"
            ]
          },
          "implementation_notes": "WALKING SKELETON (@walking_skeleton @wiring_e2e @us-mwt01). Greens scenario #1 (the core hypothesis). FILENAME DIVERGENCE (load-bearing — honor the REPO, not the prose): the DISTILL/slice/ADR-006 docs colloquially name this `0002_multi_workspace.sql`, but the shipped tree ALREADY has 0001..0008 (0002_sessions_and_reset.sql exists). `sqlx::migrate!(\"./migrations\")` (foundry-store/src/lib.rs:122,1589) iterates by version number, so the next free forward migration is 0009 — author it as crates/foundry-store/migrations/0009_multi_workspace.sql. Using `0002` would collide and is WRONG. THE ONLY GENUINELY-NEW PRODUCTION SURFACE THIS SLICE NEEDS: this one-statement index drop. Everything below the migration is SHIPPED-REUSE: the GET .../issues route + list_issues_handler (foundry-api US-W05a/b), the MachinePrincipal extractor whose token.workspace_id is the acting workspace (foundry-api/src/lib.rs:583-588, ADR-001 API leg), Services::list_board_issues' membership authz + per-table workspace_id scoping (foundry-services/src/lib.rs:326-372), the InProcHarness + testcontainers PG16 + mint_bearer_bound — ALL ALREADY SHIP. The step's production work is the migration; the .feature edit is keeping scenario #1's @walking_skeleton un-skipped (it already is). bootstrap.rs:289 409 guard is NOT touched: the fixture INSERTs the workspace row directly via sqlx and never hits create_workspace (upstream-changes.md Finding 1 — the 409 removal is Slice 6, verified out of this path). ACTINGWORKSPACE NOTE (ADR-002): NOT introduced in this slice — the resolution seam slice 1 exercises is the shipped token.workspace_id via principal.workspace_id(); ActingWorkspace + its check-arch LAYER-1e guard earn their keep once multiple surfaces exist (deferred to Slice 2 per ADR-002 slice-alignment; for a walking skeleton, smallest surface that proves the hypothesis). TEST PARADIGM: LAYER-3 real-adapter acceptance (real HTTP + real Postgres + real EdDSA), example-based per Mandates 9+11 — NO @given/state-machine PBT machinery; Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: real-I/O acceptance verifies wiring/isolation end-to-end, not a quantified invariant over a domain-rich input space; the contract is a fixed (200 + only-own-keys) outcome. Compensate: the migration's no-rewrite contract is probed behaviorally by scenario #6 (step 01-02) and as a user-guarantee in Slice 5 (US-MWT06, real pre-feature DB). EARNED-TRUST (Principle 12): the migration is a driven-adapter dependency on Postgres index semantics — scenario #1's REAL second-INSERT-then-isolated-read IS the empirical probe that dropping the index actually lets two tenants coexist WITHOUT bleeding data (asking 'what happens once a second tenant really exists?'), not a faith-based DROP INDEX. RED entry signal: today the second INSERT fails on uniq_one_workspace (MISSING_FUNCTIONALITY, not BROKEN); the crate compiles clean (cargo test -p foundry-acceptance --no-run = Finished)."
        },
        {
          "id": "01-02",
          "name": "Coexistence hardening over the migration: disjoint A/B sets, second-workspace creation, empty-new-workspace, and migration no-rewrite",
          "criteria": [
            "with 0009 applied, two bearers bound to Acme and Globex respectively each list their own project and the Acme answer contains only ACME- keys, the Globex answer only GLOBEX- keys, and neither answer contains any of the other workspace's issues (disjoint sets, scenario #2)",
            "a second `workspaces` row (\"Globex\") is created alongside an instance that already has \"Acme\" and both workspaces exist on the instance (COUNT(*) >= 2), with neither creation blocked by a single-workspace limit — the coexistence guarantee the index drop unlocks (scenario #3)",
            "a brand-new workspace (Globex) whose project has issues, listed by its own bound credential, returns 200 with an empty data list when the targeted project is genuinely empty, and no neighbour's (Acme's) issues appear — a new workspace starts empty, not populated from a neighbour (scenario #4)",
            "after the single-workspace guard is dropped, the pre-existing Acme workspace's id is unchanged and every Acme issue recorded beforehand is present and unchanged afterward (count-equal over `workspace_id`) — the forward-only migration rewrites no existing data (scenario #6, NFR-MWT-DATA-01/02)",
            "all four scenarios run at LAYER 3 against the real two-workspace fixture (real members, tokens, issues) and add ZERO new production surface beyond the 01-01 migration — they assert properties of the shipped scoping + the dropped index over the real transport"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-01-coexist.feature",
          "scenario_name": "Each workspace's members see a disjoint set of data | A second workspace can be created where none could before | A brand-new workspace starts empty, not populated from a neighbour | Dropping the single-workspace guard leaves the existing workspace's data unchanged",
          "estimate_hours": 3,
          "deps": ["01-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-acceptance/tests/features/us-mwt-slice-01-coexist.feature"
            ]
          },
          "implementation_notes": "Greens scenarios #2 (disjoint A/B), #3 (@coexistence second workspace), #4 (empty new workspace), #6 (@migration @no-rewrite). NO NEW PRODUCTION CODE: every one of these is a behavioral consequence of the 01-01 migration plus SHIPPED machinery — the per-table workspace_id scoping in Services::list_board_issues (disjoint + empty), the dropped index (coexistence COUNT>=2), and the no-data-touch nature of DROP INDEX (no-rewrite). The step's work is purely un-skipping these four @pending scenarios one at a time (one-at-a-time DELIVER cycle: flip ONE @pending per RED->GREEN->COMMIT, in the order #3 coexistence -> #2 disjoint -> #4 empty -> #6 no-rewrite, smallest-blast-radius first) and confirming each holds over the real transport; files_to_modify is the .feature only (remove @pending). The no-rewrite scenario #6 is the BEHAVIORAL probe of ADR-006's contract at slice scope (its full user-guarantee against a real pre-feature DB snapshot is Slice 5 / US-MWT06 — do NOT pull that forward). bootstrap.rs:289 409 is NOT touched here: scenario #3's 'created alongside' seeds via the additive sqlx fixture (workspace_created_alongside -> workspace_exists_with_admin), which never calls create_workspace (upstream-changes.md Finding 1; the product create-workspace flow + 409 removal is Slice 6). The two un-scoped reads (invites WHERE id, teams WHERE id, upstream Finding 2) are NOT on the issue-list path and are Slice 2/6 — do NOT pull forward. TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9+11), Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: these are fixed isolation/coexistence/equality outcomes over real I/O, not quantified properties over a domain-rich space; sad/edge paths (empty, no-rewrite) are enumerated as distinct scenarios — the correct instrument for an isolation boundary. Compensate: scenario #6's before/after id-equality + issue-count-equality is itself a tight equality probe of the migration's no-rewrite contract. EARNED-TRUST (Principle 12): #2/#4 are the empirical probe that tenant A's data cannot bleed into B's answer (and vice versa) under two REAL coexisting tenants — the leak risk is the risk register's top-rated risk; #6 probes that the index drop does not silently rewrite the incumbent tenant's rows."
        },
        {
          "id": "01-03",
          "name": "Fail-closed resolution: a credential whose holder resolves to no workspace is refused, not defaulted",
          "criteria": [
            "a request carrying a credential whose holder belongs to NO workspace (a registered bearer for a user with no membership) listing the Acme \"Auth\" project's issues is REFUSED with a fail-closed status (401/403/404), never served against an arbitrary workspace's data (scenario #5, ADR-001 fail-closed contract, NFR-MWT-SEC-03)",
            "the refusal body contains no issue data — no ACME- and no GLOBEX- key leaks to the orphan caller",
            "the refusal is DETERMINED from the shipped seam: Services::list_board_issues resolves the team by principal.workspace_id() (the token.workspace_id claim) then gates on Store::is_team_member(team_id, principal.user_id()); the orphan holder has NO membership, so the SHIPPED membership check returns Forbidden (403) BEFORE any issue fetch — confirm this empirically; add a guard ONLY if the assertion reveals the shipped path does NOT already fail closed",
            "no adapter-side authz is added to foundry-api for this slice (the gate stays in the use-case's membership check; the boundary guard's api-no-adhoc-authz rule stays green)"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-01-coexist.feature",
          "scenario_name": "A request that resolves to no workspace is refused, not defaulted",
          "estimate_hours": 2,
          "deps": ["01-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-acceptance/tests/features/us-mwt-slice-01-coexist.feature"
            ]
          },
          "implementation_notes": "Greens scenario #5 (@us-mwt00 @error, the fail-closed edge). EXPECTED NO NEW PRODUCTION CODE — the fail-closed refusal is ALREADY ENFORCED by the shipped path, verified at design time: the orphan fixture (credential_no_workspace) mints a real registered bearer whose token.workspace_id points at a real workspace (so the MachinePrincipal extractor AUTHENTICATES — auth is not the gate), but the holder has NO workspace_memberships row. Services::list_board_issues (foundry-services/src/lib.rs:326-361) resolves find_team_by_slug(principal.workspace_id(), 'Backend') (found), then is_team_member(team.id, principal.user_id()) returns false -> ServiceError::Forbidden -> 403 via the shipped status_for envelope, BEFORE list_issues_by_project runs. So the holder-with-no-membership is refused 403 and served no data: fail-closed holds on the shipped API leg with zero new code. The step's work is un-skipping scenario #5 (remove @pending) and asserting the refusal (request_refused accepts 401/403/404; not_served asserts no ACME-/GLOBEX- key). CONTINGENCY (Earned Trust, do not assume): if the empirical run shows the shipped path does NOT fail closed (e.g. resolution somewhere defaults to a workspace), THEN and only then scope a minimal guard — most likely in the resolution seam, NOT a new adapter authz path — and record it; files_to_modify currently lists only the .feature on the design-validated expectation that no production change is needed. NOTE: this slice's fail-closed is the API leg's membership gate; the ActingWorkspace newtype + its provenance guard (ADR-002) are NOT needed to fail closed here and are deferred to Slice 2 (see 01-01 note). TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9+11), Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: the contract is a fixed (refused, no-data) outcome for a single adversarial example (the orphan), a security-boundary refusal, not a quantified property; enumerated as its own scenario is the right instrument. Compensate: list_board_issues' own membership-authz path is already exercised by the shipped Feature-A scenarios. EARNED-TRUST (Principle 12, load-bearing): scenario #5 IS the probe that the resolution seam refuses rather than defaults when the world hands it a holder that resolves to no workspace — 'what happens when the credential lies about belonging somewhere?' is answered empirically, not on faith; a request that cannot resolve to exactly one workspace must be refused (ADR-001), and this scenario proves the shipped path honors that."
        }
      ]
    },
    {
      "id": "02",
      "name": "Slice 2 — web-tier tenant-scoped authz + non-enumerable refusal (htmx boundary)",
      "steps": [
        {
          "id": "02-01",
          "name": "Web walking skeleton: session active-workspace resolution (ADR-005) so a member of Acme browsing the web sees only Acme's board",
          "criteria": [
            "with 0009 applied (the slice-1 index drop, shared pre-requisite) the Background's TWO workspaces (Acme + Globex) coexist, and a real cookie sign-in for marco@acme.com over the production session + double-submit CSRF path resolves the session's ACTIVE workspace to Acme — not an arbitrary row — by replacing the `first_workspace()` call-site (crates/foundry-app/src/signin.rs:140) with a membership lookup",
            "a NEW thin store query `memberships_for_user(user_id)` (foundry-store) drives the resolution: exactly-one membership auto-resolves to that workspace; the resolved active workspace is stamped into `SessionUser.workspace_id` (crates/foundry-app/src/session.rs); the sign-in security contract (argon2id, brute-force delay, non-enumerable error, 30-day cookie, CSRF) is UNCHANGED — selection happens AFTER verification (ADR-005, NFR-MWT-DATA-02)",
            "GET /team/{team}/project/{project} for the Acme \"Auth\" board, authenticated by the real `foundry_session` cookie, returns 200 and renders ONLY Acme issue keys (ACME-1, ACME-2); no Globex (GLOBEX-) key appears (scenario #1, the demo-able core hypothesis on the web read path)",
            "per-request `SessionUser` resolution still re-checks membership in the active workspace so a revoked membership fails closed (ADR-005); the session stays thin (carries user_id + active workspace_id, role re-checked per request)",
            "the @walking_skeleton scenario runs at LAYER 3 against the real in-process axum router (InProcHarness), real tower-sessions Postgres store, real CSRF, and real testcontainers PG16 with the per-scenario schema set — proving 'two real workspaces coexist and the web session resolves to exactly its own workspace on one read path'"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature",
          "scenario_name": "A member sees only their own workspace's board on the web",
          "estimate_hours": 5,
          "deps": ["01-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-app/src/signin.rs",
              "crates/foundry-app/src/session.rs",
              "crates/foundry-store/src/lib.rs",
              "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature"
            ]
          },
          "implementation_notes": "WALKING SKELETON (@walking_skeleton @wiring_e2e @us-mwt02). Greens scenario #1 — the slice-2 demo-able hypothesis on the web read path. DEPENDS ON 01-01: the 0009 migration (DROP INDEX uniq_one_workspace) is the shared pre-requisite that lets the Background's second `workspaces` row (Globex) exist; it ALREADY ships from slice 1, so this step does NOT re-author it (DISTILL pre-requisite #1 'satisfied if slice 1 already shipped'). THE GENUINELY-NEW PRODUCTION WORK THIS STEP: the ADR-005 session active-workspace resolution. Today signin.rs:140 calls `state.store.first_workspace()` which, under TWO coexisting workspaces, picks an ARBITRARY row — so a member of Acme is not reliably scoped to Acme (the RED edge, DISTILL fail-for-right-reason #1). Replace that call-site with a NEW thin `memberships_for_user(user_id)` store query (EXTEND the seam, do NOT refactor Store wholesale — ADR-002 rejects option (b)); single-membership auto-resolves and stamps `SessionUser.workspace_id` (session.rs). SHIPPED-REUSE below the resolution: the GET /team/{team}/project/{project} board route + the web board view (projects.rs), the `find_team_by_slug(workspace_id, …)` → `is_team_member` → `find_project_by_slug` scoping chain (already workspace-scoped on every web path — DISTILL upstream: no un-scoped web query exists), the tower-sessions Postgres store + double-submit CSRF, the InProcHarness + testcontainers PG16, sign_in_cookie. ACTINGWORKSPACE NOTE (ADR-002): the newtype landed in Slice 1; this step CONSUMES the session-resolved active workspace but the check-arch LAYER-1e guard RULE lands in 02-03 (where the path-parsed-id threat the rule defends against first appears). multi-membership (>=2) resolution is NOT exercised here (scenario #1 is single-membership marco); the full single-vs-multi-vs-zero matrix is Slice 3 (US-MWT04, ADR-005 slice-alignment) — do NOT pull forward; this step wires single-membership auto-resolve which is all scenario #1 needs. TEST PARADIGM: LAYER-3 real-adapter acceptance (real HTTP + real Postgres + real session/CSRF), example-based per Mandates 9+11 — NO @given/state-machine PBT; Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: real-I/O web acceptance verifies wiring/isolation end-to-end, not a quantified invariant over a domain-rich input space; the contract is a fixed (200 + only-own-keys) outcome over fixed Acme/Globex personas. Compensate: the resolution seam's fail-closed + multi-membership branches are quantified-example-enumerated in Slice 3. EARNED-TRUST (Principle 12): scenario #1's REAL cookie-sign-in-then-scoped-read IS the empirical probe that the web session resolves to the member's OWN workspace under two real tenants — answering 'once a second tenant really exists, does the browser session still scope correctly?', not a faith-based resolution swap. RED entry signal: the crate COMPILES (DISTILL: `cargo test -p foundry-acceptance --no-run` = Finished, not BROKEN); at runtime the resolved-to-Acme read fails because `first_workspace()` resolves arbitrarily (MISSING_FUNCTIONALITY)."
        },
        {
          "id": "02-02",
          "name": "Web read + write isolation on the acting workspace: issue-detail read and file-issue write land only in Acme",
          "criteria": [
            "GET /team/{team}/project/{project}/issues/{n} for an Acme issue (ACME-1) renders 200 with only Acme data and no Globex (GLOBEX-) key — issue detail is scoped to the session's acting workspace exactly as the board read is (scenario #2)",
            "POST /team/{team}/project/{project}/issues filing \"Rotate signing keys\" into the Acme \"Auth\" project (real CSRF, signed-in cookie) creates the issue EXACTLY ONCE in Acme (`SELECT COUNT(*) FROM issues WHERE workspace_id = Acme AND title = 'Rotate signing keys'` = 1) and ZERO rows in any other workspace — the write is scoped to the acting workspace (scenario #3, post-write workspace-scoped row-count observable)",
            "both the read and write resolve their target through the session's ActingWorkspace (the 02-01 resolution), never a path-parsed id; the issue-detail view and the file-issue submit handler (crates/foundry-app/src/issues.rs) consume the acting workspace and reuse the shipped `find_team_by_slug(workspace_id, …)` scoping chain",
            "both scenarios run at LAYER 3 over real HTTP against the two-workspace fixture and add NO new scoping surface beyond consuming the 02-01 acting workspace — they assert the shipped per-table `workspace_id` scoping holds on the web read-detail and write paths under two real tenants"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature",
          "scenario_name": "A member reads only their own workspace's issue detail on the web | A member's write affects only their own workspace on the web",
          "estimate_hours": 3,
          "deps": ["02-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-app/src/issues.rs",
              "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenario #2 (issue-detail read isolation) and #3 (file-issue write isolation). MINIMAL-TO-NO NEW PRODUCTION CODE: both are behavioral consequences of 02-01's session active-workspace resolution plus the SHIPPED web scoping — the issue-detail view + the file-issue submit handler (issues.rs:128-172) already resolve through `find_team_by_slug(workspace_id, …)` → `is_team_member` → `find_project_by_slug`, which is workspace-scoped on every web path (DISTILL upstream: no un-scoped web query exists). The ONLY change, if any, is ensuring these two handlers consume the session-resolved ActingWorkspace rather than re-deriving the workspace — wire the acting workspace through if not already; files_to_modify lists issues.rs on that contingency, but if the handlers already take the resolved workspace the step's work is purely un-skipping the two @pending scenarios. One-at-a-time DELIVER cycle: flip ONE @pending per RED->GREEN->COMMIT, order #2 read then #3 write (read is the smaller blast radius). The write assertion is a port-observable post-write workspace-scoped row count (the step-def already snapshots COUNT in Acme vs not-Acme) — Mandate 8 traditional assertion over a DB observable at layer 3. Comment/attachment/state-change writes share the SAME scoping chain (DISTILL); enumerating every write path is Slice 4's adversarial matrix, NOT slice 2 — do NOT pull forward. TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9+11), Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: fixed isolation outcomes (only-own-keys read; exactly-one-in-acting-zero-elsewhere write) over real I/O, not quantified properties over a domain-rich space. Compensate: the write's exactly-1-here / 0-elsewhere row count is itself a tight equality probe of write scoping. EARNED-TRUST (Principle 12): #3 is the empirical probe that a web write CANNOT land in a neighbour's workspace under two real tenants (the leak risk is the register's top risk); #2 probes read-detail scoping on a second representative read path. RED entry: crate compiles; at runtime both fail because the session resolves arbitrarily until 02-01 lands (MISSING_FUNCTIONALITY)."
        },
        {
          "id": "02-03",
          "name": "Non-enumerable foreign-resource refusal (security core): ActingWorkspace newtype threading + the NEW check-arch LAYER-1e tenant-scoping guard + uniform 404 for foreign board/issue/write",
          "criteria": [
            "reaching another workspace's board by its REAL address (GET /team/{globex-team}/project/{globex-project} while acting on Acme) returns HTTP 404 with a page body BYTE-IDENTICAL to a never-existed board (GET /team/no-such-team/project/no-such-project) — same status AND same page shape, so there is no enumeration oracle (scenario #4, ADR-003)",
            "reaching another workspace's issue by its real address is refused identically to a never-existed issue under the acting workspace's own project (scenario #5); writing into another workspace's project is refused identically to a write into a never-existed project AND creates ZERO Globex rows (scenario #6) — all three generalize the shipped `find_*_in_workspace → None` idiom so foreign-id ≡ missing-id (ADR-003)",
            "an `ActingWorkspace(Uuid)` newtype is threaded into the web board/issue/write handlers (crates/foundry-app/src/{projects.rs,issues.rs}) so the scoped lookup consumes ONLY the session-resolved acting workspace, never a path/query/body-parsed Uuid (ADR-002 mechanism 1) — a client-supplied workspace id becomes a type error at the call boundary",
            "a NEW check-arch LAYER-1e tenant-scoping rule is added to xtask/src/check_arch.rs that flags an adapter constructing a tenant-scoped workspace id from request input (a `Uuid::parse` of a path/query param fed into a `*_in_workspace`/workspace-scoped store call) instead of from the resolved `ActingWorkspace` — mirroring the shipped no-mint + ad-hoc-authz detectors (one detector fn)",
            "the guard ships WITH a Principle-12c gold test that PLANTS an un-scoped tenant query and asserts `check-arch` exits non-zero (the guard that verifies the guard), mirroring the shipped api≠mint planted-violation gold test; the rule carries a provisioning allow-list escape hatch (ADR-002 risk mitigation, ADR-004) so a legitimate literal-id admin path is not blocked",
            "all three refusal scenarios run at LAYER 3 over real HTTP with foreign-id and missing-id executing the SAME `WHERE id AND workspace_id` query (timing-equivalence by construction, ADR-003) — the slice asserts status + body identity; the full timing/shape adversarial matrix across ALL surfaces is Slice 4"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature",
          "scenario_name": "Reaching another workspace's board by its real address is refused non-enumerably | Reaching another workspace's issue by its real address is refused non-enumerably | Writing into another workspace's project is refused non-enumerably",
          "estimate_hours": 6,
          "deps": ["02-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-app/src/projects.rs",
              "crates/foundry-app/src/issues.rs",
              "xtask/src/check_arch.rs",
              "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenarios #4, #5, #6 (@error, the non-enumerable refusal SECURITY CORE) AND lands the ADR-002 structural seam. STRUCTURAL DECISION (recommended, called out per task): the ActingWorkspace newtype threading + the NEW check-arch LAYER-1e guard rule are their OWN step HERE, NOT folded into a single scenario and NOT spread across 02-01/02. RATIONALE: (a) ADR-002 slice-alignment puts the guard RULE with Slice 2; (b) the threat the guard defends against — a handler scoping by a path/query-PARSED id instead of the resolved acting workspace — FIRST becomes load-bearing exactly here, where scenarios #4-6 reach a FOREIGN resource by its REAL address (a path-supplied foreign id MUST NOT resolve); (c) the newtype + guard are the structural counterpart to all three behavioral refusals, so co-locating them keeps the seam and its proof in one RED->GREEN->COMMIT. GOLD-TEST OBLIGATION (Principle 12c, MANDATORY, do NOT defer): the guard ships WITH an acceptance gold test that PLANTS an un-scoped tenant query and asserts `check-arch` exits non-zero — mirror the shipped api≠mint detector's planted-violation gold test in the acceptance gold-test harness. ADR-002 §Slice-alignment says the guard's gold test 'is Slice 3+'; HONOR Principle 12c over that aside — a guard introduced without its own bite-proof is a faith-based guard (the exact dependency Principle 12 refuses). If the harness genuinely cannot host the gold test this slice, record an explicit `# bypass:` with the Slice-3 gold-test ticket; default is to ship it now. SHIPPED-REUSE: the `find_*_in_workspace → None` idiom (attachments.rs `find_attachment_in_workspace`, the canonical ADR-003 idiom), `find_team_by_slug(workspace_id, …)` (returns None for a foreign team slug once the acting ws is correctly Acme), the shipped web not-found page (the same page a never-existed id renders). NEW production: the ActingWorkspace threading into projects.rs/issues.rs handler signatures (mechanical, ADR-002) + the check_arch.rs detector fn + gold test. The uniform-404 contract is the load-bearing decision: a foreign board/issue/write is the SAME 404 (status + page body) as a never-existed one — a 403-vs-404 difference is an existence oracle (ADR-003, DISTILL uniform-refusal decision). The step-defs already assert byte-identical status+body (`refused_identically`) and zero foreign rows on the write. One-at-a-time: flip #4 (board read refusal) -> #5 (issue read refusal) -> #6 (write refusal). TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9+11), Tier-A-only per DISTILL — every evil-user path enumerated as its own scenario. EXEMPT FROM PARADIGM: a cross-tenant refusal is a fixed (404 + identical-body + no-foreign-row) outcome for adversarial examples, a security-boundary refusal, not a quantified property; enumerated scenarios are the right instrument. Compensate: the check-arch gold test IS a structural quantified guard (every adapter call-site checked at build time), and the foreign≡missing body-identity assertion is a tight equality probe. EARNED-TRUST (Principle 12, load-bearing): scenarios #4-6 are the probe that a member reaching a FOREIGN resource by its real address learns NOTHING — not even that it exists — answering 'what does an evil user with a crafted/stale cross-tenant link see?' empirically; the check-arch gold test is the probe that 'every future handler remembers to scope' is enforced at build time, not trusted. RED entry: crate compiles; at runtime #4-6 fail until the acting workspace is correctly Acme (02-01) AND the scoped lookup refuses the foreign slug non-enumerably (MISSING_FUNCTIONALITY); check-arch reds on the planted violation until the detector lands."
        },
        {
          "id": "02-04",
          "name": "Admin authority does not cross tenants: an Acme admin cannot revoke a Globex credential — non-enumerable 404, foreign credential unchanged",
          "criteria": [
            "priya@acme.com, signed in and acting on Acme, attempting POST /admin/tokens/{globex-jti}/revoke for a Globex `machine_tokens` credential is refused with a non-enumerable HTTP 404 — IDENTICAL to a never-existed jti — never a 403 or 200 that would confirm the foreign credential exists (scenario #7, ADR-003 + NFR-MT-SEC-03)",
            "the Globex credential is UNCHANGED by the attempt: its `revoked_at` is still NULL afterward (before/after equality on `SELECT (revoked_at IS NOT NULL) FROM machine_tokens WHERE jti = …`), and no Globex membership or credential is altered",
            "the refusal is determined by scoping the admin action through the acting workspace: the shipped `is_workspace_admin(acting_ws, user)` authz gate plus the revoke path's workspace-scoped jti lookup collapse non-admin / missing / foreign-jti to the SAME non-enumerable 404 (crates/foundry-app/src/admin_tokens.rs:48,188 already does this) — slice 2 proves it holds when \"foreign\" means a genuinely-coexisting second workspace",
            "no adapter-side ad-hoc authz is added to satisfy this (the gate stays the shipped `is_workspace_admin`; the boundary guard's api-no-adhoc-authz rule stays green), and the scenario runs at LAYER 3 over real HTTP"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature",
          "scenario_name": "An admin of one workspace cannot manage another's credentials on the web",
          "estimate_hours": 3,
          "deps": ["02-03"],
          "implementation_scope": {
            "files": [
              "crates/foundry-app/src/admin_tokens.rs",
              "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenario #7 (@error, admin-authority-does-not-cross). EXPECTED MINIMAL-TO-NO NEW PRODUCTION CODE: the shipped `/admin/tokens/{jti}/revoke` web handler already collapses non-admin / missing / foreign jti to the SAME non-enumerable 404 (admin_tokens.rs:48,188; NFR-MT-SEC-03) — this slice proves that idiom holds when 'foreign' means a genuinely-coexisting second workspace (DISTILL pre-requisite #5). The ONLY change, if any, is ensuring the revoke path scopes its jti lookup + the `is_workspace_admin` gate through the session-resolved acting workspace (Acme), so a Globex jti is NotFound under Acme's scope; if the handler already scopes by the acting workspace the step's work is purely un-skipping scenario #7. files_to_modify lists admin_tokens.rs on that contingency. CROSS-TENANT-NOT-INTRA NUANCE (DISTILL reconciliation): Priya acting on Acme reaching a Globex credential is a CROSS-tenant reach → the non-enumerable 404, NOT a 403; intra-workspace authz failures (a non-admin of her OWN workspace) keep their shipped 403 shape (ADR-003 boundary clause). The step-def asserts 404 (`admin_refused_non_enumerably`) AND foreign-credential-unchanged (before/after `revoked_at` equality). DEPENDS ON 02-03: reuses the acting-workspace scoping seam + the uniform-404 idiom that 02-03 generalizes; sequenced after it so the admin refusal builds on the proven resource-refusal seam. NO new adapter authz — the gate stays the shipped use-case `is_workspace_admin` (the api-no-adhoc-authz boundary rule stays green). TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9+11), Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: a fixed (404 + foreign-credential-unchanged) outcome for a single adversarial example (the cross-tenant admin), a security-boundary refusal, not a quantified property. Compensate: the before/after revoked_at equality is a tight no-mutation probe of the foreign credential. EARNED-TRUST (Principle 12, load-bearing): scenario #7 is the probe that per-tenant ADMIN authority does not cross (NFR-MWT-SEC-04) — 'can an admin of A touch B's credentials by addressing B's jti directly?' answered empirically; the answer must be a non-enumerable 404 with B's credential untouched. RED entry: crate compiles; at runtime fails until the acting workspace is Acme (02-01) and the revoke path refuses the foreign jti as NotFound under Acme's scope (MISSING_FUNCTIONALITY)."
        },
        {
          "id": "02-05",
          "name": "Multi-membership active workspace + the /workspace/switch switcher: a contractor acts on exactly one workspace and switching changes which tenant's data the web shows",
          "criteria": [
            "a contractor (dana@contract.dev) who is a member of BOTH Acme and Globex, signed in acting on Acme, opening the Acme board sees ONLY Acme data and no Globex data — the session resolves to exactly the active workspace, not an arbitrary one of the two memberships (scenario #8, OD-2 web expression)",
            "a NEW `POST /workspace/switch` route (crates/foundry-app/src routing + an Askama template/fragment) verifies the user IS a member of the target workspace, then re-stamps `SessionUser.workspace_id` to the chosen active workspace WITHOUT re-authenticating (ADR-005 switcher)",
            "after switching the active workspace to Globex, opening the Globex \"Core\" board renders ONLY Globex data (GLOBEX- keys) and NO Acme (ACME-) data — switching changes which tenant's data the web shows (scenario #9)",
            "the switch fails closed if the user is not a member of the target workspace (membership verified before re-stamping, ADR-005); both scenarios run at LAYER 3 over real HTTP reusing the slice-1 member+team+project seed via the delegating `is also a member of …` step (no copy-pasted fixture setup, Pillar 2)"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature",
          "scenario_name": "A multi-membership user acts on exactly their active workspace on the web | Switching the active workspace changes which workspace's data is shown",
          "estimate_hours": 5,
          "deps": ["02-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-app/src/session.rs",
              "crates/foundry-app/src/routes",
              "crates/foundry-acceptance/tests/features/us-mwt-slice-02-web-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenario #8 (multi-membership acts-on-active) and #9 (switch changes tenant). NEW PRODUCTION WORK: the `POST /workspace/switch` switcher route (ADR-005) — absent today, so scenario #9's switch POST currently 404s (DISTILL fail-for-right-reason #9). The route verifies the user is a member of the target workspace (reuse `memberships_for_user`/`is_team_member`-style membership check), then re-stamps `SessionUser.workspace_id` (session.rs) WITHOUT re-authenticating; add the route to foundry-app routing (crates/foundry-app/src/routes) + a small Askama switcher control/fragment. Scenario #8 exercises the 02-01 resolution under a MULTI-membership user: dana belongs to BOTH workspaces, so `first_workspace()` (or even a naive single-membership resolve) is arbitrary — the session must resolve to the INTENDED active workspace (Acme). NOTE on slice boundary: this step wires the multi-membership active-workspace resolution + switcher at the WEB level only; the full single-vs-multi-vs-zero sign-in selection matrix (US-MWT04, incl. the explicit selection prompt + zero-membership refusal) is Slice 3 (ADR-005 slice-alignment) — do NOT pull forward; scenario #8 needs only that a multi-membership session acts on its stamped active workspace, and #9 needs the switcher re-stamp. SHIPPED-REUSE: the slice-1 member+team+project seed via the delegating `is also a member of …` step (Pillar 2 — no copy-pasted setup), the board read path from 02-01, the per-request membership re-check. DEPENDS ON 02-01 (the resolution seam it extends with multi-membership + switch). One-at-a-time: #8 (acts-on-active, no new route needed beyond resolution) then #9 (switch — needs the new route). TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9+11), Tier-A-only per DISTILL — the 2-scenario switch chain is not domain-rich (fixed Acme/Globex), so Tier-A examples cover it (Mandate 10: no state-machine PBT added). EXEMPT FROM PARADIGM: fixed (only-active-ws-data; post-switch-only-new-ws-data) outcomes over real I/O for the fixed contractor persona, not a quantified property. Compensate: the post-switch only-Globex / no-Acme assertion is a tight before/after tenant-flip probe. EARNED-TRUST (Principle 12): #8 probes that a multi-membership session acts on EXACTLY its active workspace (no silent default, ADR-005 rejects option (c)); #9 probes that the switcher genuinely re-scopes subsequent reads — 'does switching actually change which tenant the browser sees?' answered empirically. RED entry: crate compiles; #8 fails until multi-membership resolution stamps the intended active workspace; #9 fails because /workspace/switch does not exist (404) and the post-switch read is mis-scoped until the switcher re-stamps (MISSING_FUNCTIONALITY)."
        }
      ]
    },
    {
      "id": "03",
      "name": "Slice 3 — API + machine-token + session-resolution boundary (residual closure + verify-path regression)",
      "steps": [
        {
          "id": "03-01",
          "name": "API walking skeleton: an Acme-bound token's WRITE lands only in Acme, and its READ returns only Acme — confinement on the JSON /api/v1 write+read paths",
          "criteria": [
            "with the shipped 0009 migration applied, the slice-3 Background's TWO workspaces (Acme + Globex) coexist, and a real EdDSA bearer bound to (marco, Acme) — minted by the reused slice-1 `a machine credential is bound to ...` Given — files \"Rotate signing keys\" into the Acme \"Auth\" project over POST /api/v1/teams/{team}/projects/{project}/issues and the write is reported created (201/200) (scenario #1, the @walking_skeleton WRITE-confinement hypothesis)",
            "after the write, `SELECT COUNT(*) FROM issues WHERE workspace_id = Acme AND title = 'Rotate signing keys'` >= 1 and the SAME-title count in Globex is 0 — the API write is scoped to the acting workspace (token.workspace_id), creating zero rows in any neighbour (scenario #1 post-write workspace-scoped row count)",
            "the SAME Acme bearer listing the \"Auth\" project's issues over GET /api/v1/.../issues receives 200 with ONLY the Acme issues (ACME-1, ACME-2) and no Globex (GLOBEX-) key — the API read is confined to token.workspace_id under two real tenants (scenario #2, the slice-1 read path riding the slice-3 fixture)",
            "both scenarios authenticate end-to-end against the real /api/v1 router via the InProcHarness (real HTTP, real EdDSA bearer, real testcontainers PG16 with the per-scenario schema set) and add ZERO new production surface — they prove the SHIPPED `create_issue`/`insert_issue_with_outbox` acting-workspace write binding and `list_issues_by_project` read scoping hold once a second tenant truly exists"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature",
          "scenario_name": "A workspace-bound token's write lands only in its own workspace | A workspace-bound token reads only its own workspace's issues over the API",
          "estimate_hours": 3,
          "deps": ["01-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature"
            ]
          },
          "implementation_notes": "WALKING SKELETON (@walking_skeleton @wiring_e2e @us-mwt03). Greens scenario #1 (the demo-able API WRITE-confinement hypothesis — slice 1 proved READ, this is the first WRITE-path confinement) and scenario #2 (READ confinement on the slice-3 fixture). DEPENDS ON 01-01: the 0009 migration (DROP INDEX uniq_one_workspace) is the shared, ALREADY-SHIPPED pre-requisite that lets the Background's second `workspaces` row (Globex) exist — this step does NOT re-author it (DISTILL pre-requisite #1 'satisfied if slices 1-2 already shipped'; the DISTILL doc colloquially calls it `0002` but the shipped tree's next free version is `0009` — same load-bearing filename divergence resolved in 01-01). EXPECTED GREEN-BY-INHERITANCE, ZERO NEW PRODUCTION CODE: every behaviour here is a consequence of 0009 plus SHIPPED machinery — the POST/GET /api/v1 issue routes + handlers (foundry-api US-W05a/b), the MachinePrincipal bearer extractor whose token.workspace_id is the acting workspace (foundry-api/src/lib.rs, ADR-001 API leg), the `create_issue` -> `insert_issue_with_outbox` acting-workspace write binding and `list_issues_by_project` read scoping (foundry-services), the InProcHarness + testcontainers PG16 + the self-contained mint_bearer_bound helper — ALL ALREADY SHIP and are mutation-hardened. The step's work is keeping scenario #1's @walking_skeleton un-skipped (it already is) and un-skipping scenario #2 (remove @pending). files_to_modify is the .feature only. CONTINGENCY (Earned Trust, do not assume): files_to_modify lists ONLY the .feature on the design-validated expectation that no production change is needed; if the empirical run shows the API write path does NOT scope by token.workspace_id (e.g. honours a path/body-parsed workspace), THEN and only then scope a minimal foundry-api/foundry-services fix and record it — but DISTILL upstream-issues found NO un-scoped /api/v1 path. One-at-a-time DELIVER cycle: scenario #1 (@walking_skeleton, already active) is GREEN first via the 0009 coexistence + shipped write binding, THEN flip #2 (@pending->active) for the read. TEST PARADIGM: LAYER-3 real-adapter acceptance (real HTTP + real Postgres + real EdDSA), example-based per Mandates 9 + 11 — NO @given/state-machine PBT machinery; Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: real-I/O acceptance verifies wiring/isolation end-to-end, not a quantified invariant over a domain-rich input space; the contract is a fixed (created + only-Acme-rows + only-own-keys) outcome over fixed Acme/Globex personas. Compensate: scenario #1's exactly-1-in-Acme / 0-in-Globex post-write row count is itself a tight equality probe of write scoping. EARNED-TRUST (Principle 12): scenario #1's REAL Acme-bearer-POST-then-workspace-scoped-row-count IS the empirical probe that an API write CANNOT land in a neighbour's workspace under two real tenants (the leak risk is the register's top risk) — answering 'once a second tenant really exists, does the machine-token write still scope correctly?', not a faith-based binding. RED entry signal: the crate COMPILES (DISTILL: `cargo test -p foundry-acceptance --no-run` = Finished, not BROKEN); at runtime today the Background's SECOND `INSERT INTO workspaces` fails on uniq_one_workspace until 0009 is on the per-scenario schema set (MISSING_FUNCTIONALITY); once 0009 applies, both greens via the shipped scoping."
        },
        {
          "id": "03-02",
          "name": "Cross-tenant API refusal (security core): an Acme token reaching a real Globex project to READ or WRITE is refused non-enumerably — uniform 404 identical to a never-existed id, zero foreign rows",
          "criteria": [
            "the Acme bearer listing the REAL Globex \"Core\" project's issues by its real address (GET /api/v1/.../issues) returns HTTP 404 with a body BYTE-IDENTICAL (status + body) to listing a never-existed team/project — so there is no enumeration oracle distinguishing a foreign resource from a missing one (scenario #3, ADR-003 uniform-404)",
            "nothing in that refusal body reveals the Globex project exists (no GLOBEX- key, no 'core' identifier leaks to the Acme caller) (scenario #3 non-leak assertion)",
            "the Acme bearer filing \"Sneaky\" into the REAL Globex \"Core\" project by its real address (POST /api/v1/.../issues) is refused 404 byte-identically to filing into a never-existed project, AND `SELECT COUNT(*) FROM issues WHERE workspace_id = Globex AND title = 'Sneaky'` = 0 — the cross-tenant write creates zero Globex rows (scenario #4)",
            "both scenarios run at LAYER 3 over real HTTP with foreign-id and missing-id executing the SAME workspace-scoped `find_*_in_workspace`->None query (timing-equivalence by construction, ADR-003) and add NO new scoping surface — they generalize the SHIPPED `find_*_in_workspace`->None idiom + the shipped `status_for` 404 JSON envelope to the /api/v1 read+write paths under two real tenants"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature",
          "scenario_name": "A cross-workspace API read is refused non-enumerably | A cross-workspace API write is refused non-enumerably",
          "estimate_hours": 3,
          "deps": ["03-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenarios #3 (cross-tenant READ refusal) and #4 (cross-tenant WRITE refusal) — the @error API security core. EXPECTED GREEN-BY-INHERITANCE, MINIMAL-TO-NO NEW PRODUCTION CODE: both are behavioral consequences of 03-01's coexisting fixture plus the SHIPPED scoping — once the acting workspace is correctly Acme (token.workspace_id), `find_team_by_slug(Acme, 'Platform')` / `find_project_by_slug` return None for a Globex slug, so the read and write both collapse to the shipped `status_for` 404 JSON envelope, byte-identical to a never-existed id (ADR-003 option (b), generalising the canonical `find_*_in_workspace`->None idiom). The step-defs already assert byte-identical status+body (`two_api_refused_identically`), zero foreign rows on the write (`no_issue_created_in` checks both 'Rotate signing keys' and 'Sneaky' = 0 in the named ws), and non-leak (`nothing_reveals_foreign`). The UNIFORM-404 contract is load-bearing: a foreign read/write is the SAME 404 (status + body) as a never-existed one — a 403-vs-404 difference would be an existence oracle (ADR-003, DISTILL uniform-refusal decision); cross-tenant access NEVER 403s. files_to_modify lists ONLY the .feature; CONTINGENCY (Earned Trust): if the empirical run shows the foreign read/write returns anything other than the missing-id 404 (e.g. a distinguishable 403 or a leaking body), THEN and only then scope a minimal foundry-api/foundry-services fix to collapse foreign-id to missing-id and record it — but DISTILL found the shipped path already does this. One-at-a-time DELIVER cycle: flip #3 (read refusal, smaller blast radius) then #4 (write refusal). The full timing/shape adversarial matrix across ALL surfaces is Slice 4 — do NOT pull forward. TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9 + 11), Tier-A-only per DISTILL — every evil-user path enumerated as its own scenario. EXEMPT FROM PARADIGM: a cross-tenant refusal is a fixed (404 + identical-body + no-foreign-row) outcome for adversarial examples, a security-boundary refusal, not a quantified property; enumerated scenarios are the right instrument. Compensate: the foreign-id <-> missing-id body-identity assertion is a tight equality probe, and #4's 0-Globex-rows is a tight no-leak write probe. EARNED-TRUST (Principle 12, load-bearing): #3 and #4 are the probe that an Acme token reaching a FOREIGN resource by its REAL address learns NOTHING — not even that it exists — answering 'what does an Acme-bound machine token with a crafted cross-tenant link see/do?' empirically. RED entry: crate compiles; at runtime both fail until 0009 lets two workspaces coexist (Background red) and the acting-workspace scoping refuses the Globex slug non-enumerably (MISSING_FUNCTIONALITY); green via the shipped find_*_in_workspace->None / 404 envelope."
        },
        {
          "id": "03-03",
          "name": "Token list/revoke confinement (RESIDUAL CLOSURE — NFR-MWT-TEST-01 / DM8): an Acme token lists only Acme's tokens and cannot revoke a REAL Globex jti — refused 404, Globex token stays active",
          "criteria": [
            "with a REAL `machine_tokens` row 'acme-ci' in Acme and a REAL 'globex-ci' in Globex (each bound to its workspace's admin), an Acme admin bearer listing the workspace's tokens over GET /api/v1/.../tokens returns 200 with 'acme-ci' present and 'globex-ci' ABSENT — `list_tokens(principal.workspace_id())` confines the list to the acting workspace under a real cross-tenant fixture (scenario #5, residual closure replacing the synthetic-uuid us-tma proof)",
            "an Acme admin bearer revoking the REAL Globex jti (DELETE /api/v1/.../tokens/{globex-jti}) is refused 404 BYTE-IDENTICALLY to revoking a token id that exists nowhere — `revoke_token`'s `row.workspace_id != principal.workspace_id() => NotFound` collapses a real foreign jti to the same non-enumerable NotFound as a random one (scenario #6, the KEY residual-closure item)",
            "after the refused cross-tenant revoke, `SELECT revoked_at FROM machine_tokens WHERE jti = globex-ci` is STILL NULL — the Globex token remains active; the cross-tenant call mutated no foreign credential (scenario #6 before/after non-mutation assertion)",
            "both scenarios run at LAYER 3 over real HTTP against the two-workspace fixture and add NO new scoping surface — they prove the SHIPPED, 100%-mutation-hardened `list_tokens(workspace_id)` + `revoke_token` cross-tenant NotFound behaviour holds against a REAL foreign row, converting the accepted us-tma synthetic-uuid residual (docs/evolution/2026-06-08-token-management-api.md, UI-1) into a real-fixture proof"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature",
          "scenario_name": "A workspace-bound token lists only its own workspace's tokens | A workspace-bound token cannot revoke another workspace's token",
          "estimate_hours": 3,
          "deps": ["03-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenario #5 (token LIST confined) and #6 (token REVOKE confined — the KEY item) — the @error RESIDUAL-CLOSURE pair. THIS IS THE RESIDUAL-CLOSING REAL-FIXTURE PROOF (NFR-MWT-TEST-01 / DM8): the shipped `token-management-api` (us-tma) tests cross-workspace non-enumerability with a SYNTHETIC random uuid as the 'foreign' jti (feature_token_management_api::credential_in_another_workspace records `uuid::Uuid::now_v7()` with the comment 'Single-workspace model (uniq_one_workspace ...): a real foreign workspace row is not insertable') — the accepted residual UI-1 (docs/evolution/2026-06-08-token-management-api.md). Once 0009 lets a second workspace exist, these two scenarios seed REAL Globex rows (the new `a managed token \"<label>\" exists in workspace \"<ws>\"` Given seeds a real machine_tokens row bound to the named ws's admin) and prove the confinement against a REAL foreign target, not a random uuid that simply is not in the registry. EXPECTED GREEN-BY-INHERITANCE, ZERO NEW PRODUCTION CODE: the `list_tokens(principal.workspace_id())` workspace-scoped list and `revoke_token`'s `row.workspace_id != principal.workspace_id() => NotFound` (tokens.rs:267) are ALREADY shipped AND 100%-mutation-hardened — they have simply never been exercised under a genuinely-coexisting second workspace. The step-defs already assert: list contains acme-ci / excludes globex-ci (`token_list_contains`/`token_list_excludes`), the foreign-jti revoke is 404 byte-identical to a never-existed jti (`two_revokes_identical_not_found`), and globex-ci's revoked_at is STILL NULL afterward (`foreign_token_remains_active`). files_to_modify lists ONLY the .feature; CONTINGENCY (Earned Trust): if the empirical run shows the list leaks a foreign-ws token or the foreign-jti revoke is distinguishable from a never-existed one (or mutates the Globex row), THEN and only then scope a minimal tokens.rs/foundry-services fix and record it — but the mutation-hardening makes this the least-likely place to need a change. The bearer is bound to ops@acme.com (the Acme ADMIN) so `is_workspace_admin` holds; cross-tenant reach is the non-enumerable 404, intra-workspace authz failures keep their shipped 403 shape (ADR-003 boundary clause). One-at-a-time DELIVER cycle: flip #5 (list) then #6 (revoke). TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9 + 11), Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: fixed (only-own-label present / foreign-label absent; foreign-jti 404 identical-to-missing + foreign-token-still-active) outcomes for adversarial examples over real I/O, not quantified properties. Compensate: the before/after globex-ci revoked_at = NULL is a tight no-mutation probe, and the foreign-jti <-> never-existed-jti revoke body-identity is a tight equality probe. EARNED-TRUST (Principle 12, load-bearing): #5/#6 are the probe that token list/revoke confine to the acting workspace against a REAL foreign credential — 'can an Acme token enumerate or revoke a real Globex credential by addressing its jti directly?' answered empirically with a real row, closing the synthetic-uuid gap the prior feature explicitly deferred. RED entry: crate compiles; at runtime fails until 0009 lets Globex exist (Background red, and the `managed token ... in workspace \"Globex\"` Given cannot seed a Globex row until then) — once 0009 applies, green via the shipped + mutation-hardened list_tokens/revoke_token scoping (MISSING_FUNCTIONALITY)."
        },
        {
          "id": "03-04",
          "name": "Session-resolution CONTRACT asserted directly (US-MWT04, ADR-005): single-membership auto-resolves, multi-membership resolves to the chosen one, zero-membership fails closed",
          "criteria": [
            "a single-membership user (marco@acme.com, a member of Acme only) has his session's acting workspace resolved via the SHIPPED `Store::resolve_active_workspace` seam and it resolves to EXACTLY Acme with no workspace choice step required — single-membership auto-resolution (scenario #7, US-MWT04 AC1)",
            "a multi-membership user (dana@contract.dev, a member of BOTH Acme and Globex) who has CHOSEN Globex (persisted via the shipped `set_active_workspace`) has her session resolved to EXACTLY Globex — the persisted active workspace is honoured and the session is scoped to exactly one workspace, not an arbitrary membership (scenario #8, US-MWT04 AC2)",
            "a zero-membership user (evicted@nowhere.test, a bare user row with no `workspace_memberships`) has their session resolved to NO workspace — `resolve_active_workspace` returns None, the session is not scoped to any workspace, and is refused rather than defaulted to an arbitrary tenant (scenario #9, US-MWT04 AC3, NFR-MWT-SEC-03 fail-closed)",
            "all three scenarios assert the resolution CONTRACT directly at the SHIPPED `resolve_active_workspace` / `set_active_workspace` store seam (LAYER 3, real Postgres) and add NO new production surface — distinct from slice-2's web SWITCH scenarios that exercise the same seam through the board UI; here the invariant under test is 'resolution yields EXACTLY one, fail-closed when none'"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature",
          "scenario_name": "A single-membership session resolves to exactly one workspace automatically | A multi-membership session resolves to exactly the chosen workspace | A session that resolves to no workspace is refused, not defaulted",
          "estimate_hours": 3,
          "deps": ["03-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenario #7 (single-membership auto-resolve), #8 (multi-membership resolves to the chosen one), #9 (@error zero-membership fail-closed) — the US-MWT04 session-resolution CONTRACT. EXPECTED GREEN-BY-INHERITANCE, ZERO NEW PRODUCTION CODE: the `Store::resolve_active_workspace` seam (ADR-005: single auto / multi persisted-active / zero -> None fail-closed) and `Store::set_active_workspace` were SHIPPED by slices 1-2 DELIVER (confirmed in code: foundry-store::resolve_active_workspace + ::set_active_workspace, foundry-app/src/session.rs::submit_switch, the /workspace/switch route, and signin.rs already failing closed on None — DISTILL reconciliation 'Nuance surfaced'). So US-MWT04's contract is green-by-inheritance at the store/sign-in seam; this step's job is to ASSERT that contract DIRECTLY (resolution yields exactly one + fail-closed when none) — distinct from slice-2's web SWITCH scenarios (02-05) that exercise the same seam through the board UI. The step-defs drive the seam directly: `session_resolved` calls `store.resolve_active_workspace(user_id)`; the Givens record the expected target (`belongs_to_exactly_one` asserts count==1, `has_chosen_active` calls set_active_workspace, `belongs_to_no_workspace` seeds a bare user). Thens assert the resolved id equals the expected workspace (`resolves_to_exactly`), no-choice (`no_choice_required`), exactly-one (`scoped_to_exactly_one`), and None for the evicted user (`no_workspace_resolved`/`not_scoped_to_any`). Scenario #8 reuses slice-2's `is also a member of ...` cross-seed Given (Pillar 2 — no copy-pasted setup) to make dana a member of both. files_to_modify lists ONLY the .feature; CONTINGENCY (Earned Trust): if the empirical run shows resolution defaults a zero-membership user to an arbitrary workspace (NOT None) or a multi-membership user to a non-chosen one, THEN and only then scope a minimal foundry-store resolve_active_workspace fix and record it — but DISTILL confirmed the shipped seam already implements the single/multi/none contract fail-closed. One-at-a-time DELIVER cycle: flip #7 then #8 then #9. The full single-vs-multi-vs-zero SIGN-IN selection UI matrix (the explicit selection prompt) is the web concern already covered; this step asserts the store-seam contract. TEST PARADIGM: LAYER-3 real-adapter (real Postgres via testcontainers), example-based per Mandates 9 + 11 — NO PBT machinery; Tier-A-only per DISTILL (the contract has 3 cases but the input space is not domain-rich — fixed Acme/Globex personas — so Tier A examples cover it, Mandate 10: no state-machine PBT added). EXEMPT FROM PARADIGM: fixed (resolves-to-exactly-X / None) outcomes over real I/O for fixed personas, not a quantified property over a domain-rich space; the three cases (single/multi/none) are enumerated as distinct scenarios — the correct instrument for a resolution contract. Compensate: each scenario's resolved-id == expected-id (or is-None) is a tight equality probe of the resolution seam. EARNED-TRUST (Principle 12, load-bearing): #7 probes single-membership auto-resolves with no prompt; #8 probes the persisted choice is honoured (no silent default, ADR-005 rejects option (c)); #9 probes the seam REFUSES rather than defaults when the world hands it a holder that resolves to no workspace ('what happens when a session resolves to nobody's workspace?' answered empirically — a session that cannot resolve to exactly one must be refused, ADR-005 + NFR-MWT-SEC-03). RED entry: crate compiles; at runtime fails until 0009 lets the Background's two workspaces coexist (the resolution fixtures need both Acme and Globex to seed dana's dual membership) — once 0009 applies, all three green via the shipped resolve_active_workspace/set_active_workspace contract (MISSING_FUNCTIONALITY)."
        },
        {
          "id": "03-05",
          "name": "Verify-path-unchanged regression (NFR invariant): multi-workspace did NOT weaken token verification — revoked-jti and disallowed-algorithm credentials are still refused 401",
          "criteria": [
            "a machine credential bound to (marco, Acme) whose registry row has been REVOKED (its jti on the per-request denylist) making its next GET /api/v1/.../issues call is refused HTTP 401 by the verify path — the jti denylist still rejects a revoked credential under two coexisting workspaces (scenario #10, NFR verify-path-unchanged)",
            "a credential signed with a DISALLOWED algorithm (an HS256 token using the server's public-key bytes as the HMAC secret) making the same call is ALSO refused 401 — the verifier still pins exactly [EdDSA] and refuses the alg-confusion token before any key is consulted, exactly as before this feature (scenario #10, EdDSA/iss/aud pinning regression)",
            "the regression runs at LAYER 3 over real HTTP under the real Ed25519 verifier + per-request jti denylist and adds NO new production surface — it confirms the carried invariant (ADR-003 boundary clause: this slice scopes WHICH workspace a principal acts on, NOT how a token is verified); the shipped bearer 401 and the non-enumerable sign-in error are UNCHANGED"
          ],
          "test_file": "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature",
          "scenario_name": "The shipped token verify path and jti denylist are unchanged under multi-workspace",
          "estimate_hours": 2,
          "deps": ["03-01"],
          "implementation_scope": {
            "files": [
              "crates/foundry-acceptance/tests/features/us-mwt-slice-03-api-auth-boundary.feature"
            ]
          },
          "implementation_notes": "Greens scenario #10 (@error @verify-path-unchanged) — the carried-invariant regression guard. EXPECTED GREEN-BY-INHERITANCE, ZERO NEW PRODUCTION CODE: this scenario regression-guards that multi-workspace tenancy did NOT weaken the SHIPPED verify path — the per-request jti denylist (a revoked credential's next call -> 401) and the EdDSA/iss/aud pinning (a disallowed-algorithm credential -> 401) are unchanged. ADR-003 boundary clause: this slice scopes WHICH workspace a principal acts on, NOT how a token is verified; scenario #10 proves the verify path holds under two coexisting workspaces. The step-defs: `that credential has been revoked` mints+registers a fresh known-jti Acme bearer and revokes its registry row; `revoked_lists` makes its next /api/v1 issues-list call; `refused_unauthorized_verify` asserts 401 (the jti denylist); `disallowed_alg_refused` constructs an HS256 token using TEST_PUBLIC_KEY_PEM as the HMAC secret and asserts 401 (the EdDSA pinning refuses before any key is consulted — mirrors feature_token_management_api's wrong-alg construction). files_to_modify lists ONLY the .feature; this is a pure regression assertion over the shipped token_auth::authenticate path — no contingency for production work is expected (a failure here would indicate a multi-workspace change accidentally weakened verification, which DISTILL confirmed did not happen). DEPENDS ON 03-01 (shares the coexisting Background + the harness). One-at-a-time DELIVER cycle: flip #10 (the last scenario) last. TEST PARADIGM: LAYER-3 real-adapter, example-based (Mandates 9 + 11), Tier-A-only per DISTILL. EXEMPT FROM PARADIGM: a fixed (401 + 401) outcome for two adversarial examples (revoked-jti, disallowed-alg), a security-boundary refusal, not a quantified property; the verify path's exact-error contract is the right instrument. Compensate: the disallowed-alg 401 IS a tight pinning probe (the alg-confusion token must be refused before any key is consulted). EARNED-TRUST (Principle 12, load-bearing): scenario #10 is the probe that the carried verify-path invariant survives the multi-workspace change — 'did adding tenant-scoping accidentally weaken token verification?' answered empirically with a revoked credential and an alg-confusion token, not assumed. RED entry: crate compiles; at runtime fails until 0009 lets the Background's two workspaces coexist — once 0009 applies, both refusals green via the unchanged shipped verify path (jti denylist + EdDSA pinning) (MISSING_FUNCTIONALITY)."
        }
      ]
    }
  ],
  "implementation_scope": {
    "source_directories": [
      "crates/foundry-store/migrations/"
    ],
    "test_directories": [
      "crates/foundry-acceptance/tests/"
    ],
    "excluded_patterns": [
      "target/**",
      "**/*.lock"
    ]
  },
  "validation": {
    "status": "approved",
    "reviewer": "slice-by-slice-checkpoint",
    "approved_at": "2026-06-09T00:00:00Z"
  }
}
